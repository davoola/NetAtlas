# 网图·IP管家（NetAtlas）用户操作手册

本文档面向超级管理员、管理员和普通用户，说明系统登录、IP 台账、网段查询、导入导出和权限相关操作。

## 一、登录与通用操作

1. 使用浏览器访问系统地址，例如 `http://localhost:3000`。
2. 输入用户名和密码，点击“登录”。
3. 登录后通过左侧导航进入仪表板、IP 登记、网段明细、数据导入等模块。
4. 顶部栏可进入修改密码功能；完成工作后点击退出登录。
5. 所有用户都应使用个人账号，不要共用账号或转交密码。

首次安装的默认账号为 `admin / admin123`。首次登录后必须立即修改密码。

## 二、角色说明

### 1. 超级管理员

超级管理员拥有系统完整权限，可以：

- 查看和维护全部 IP、网段及 VLAN 数据；
- 新增、编辑、删除用户并分配角色；
- 为管理员授权可管理的 VLAN；
- 维护设备类型、使用状态、部门、VLAN 规划、网关映射；
- 执行数据导入、导出和系统配置；
- 查看审计日志；
- 修改自己的密码。

### 2. 管理员

管理员负责被授权 VLAN 的日常维护，可以：

- 查看仪表板、网段明细和 IP 台账；
- 新增、编辑和删除授权 VLAN 内的 IP 记录；
- 导入授权 VLAN 范围内的数据；
- 查询和导出数据；
- 查看数据字典，但不能修改；
- 修改自己的密码。

管理员不能管理用户、审计日志、系统配置或未授权 VLAN。

### 3. 普通用户

普通用户为只读角色，可以：

- 查看仪表板统计；
- 查询、筛选、排序和查看 IP 记录；
- 查看网段使用明细；
- 查看数据字典；
- 导出允许查看的数据；
- 修改自己的密码。

普通用户不能新增、编辑、删除或导入 IP 数据，也不能管理系统配置。

## 三、超级管理员操作

### 1. 用户管理

1. 进入“用户管理”。
2. 点击“新增用户”。
3. 填写用户名、初始密码和角色。
4. 保存后，将账号和临时密码通过安全方式交付给使用者。
5. 对管理员账号进入“VLAN 权限”，勾选其负责的 VLAN 后保存。
6. 用户忘记密码时，可使用“重置密码”设置临时密码。
7. 用户离职或不再使用时，应及时删除账号或撤销其权限。

建议每位工作人员使用独立账号，以保证审计日志准确。

### 2. 数据字典维护

进入“数据字典”后，可分别维护：

- 设备类型：例如服务器、交换机、打印机；
- 使用状态：例如已使用、预留/备用、已废弃；
- 所属部门；
- VLAN 规划：VLAN 编号、网段、可用地址范围；
- 网关映射：IP 前缀与网关地址；
- 系统配置：网站名称等配置项。

维护 VLAN 时，应确认网段格式正确，且可用地址范围与实际网络规划一致。修改字典前请确认不会影响现有记录的筛选、导入和统计。

### 3. 查看审计日志

1. 进入“审计日志”。
2. 可按操作人、动作、摘要、对象类型和时间范围筛选。
3. 点击日志查看详细信息。
4. 重点关注异常登录后的批量删除、批量导入、权限变更和系统配置变更。

审计日志用于追溯关键操作，业务页面不提供删除功能。

## 四、IP 台账操作

### 1. 新增 IP 记录

1. 进入“IP 登记”。
2. 点击“新增”。
3. 填写 IP 地址、设备名称、MAC 地址、部门、使用人、设备类型和使用状态等字段。
4. 输入 IP 后，系统会尝试根据前缀自动填充网关和 VLAN。
5. 检查 VLAN、网关和地址池范围是否正确。
6. 保存记录。

系统会在保存时校验：

- IP 地址格式；
- IP 是否属于所选 VLAN 的子网；
- IP 是否位于 VLAN 配置的可用地址范围；
- 字典字段是否有效；
- 当前用户是否有对应 VLAN 权限；
- 是否存在重复 IP。

网关地址可按系统规则放行，但应避免将网关误登记为普通终端。

### 2. 查看、编辑和删除

- 点击查看图标可查看记录的全部字段。
- 点击编辑图标可修改记录。
- 点击删除图标前应确认记录确实不再使用。
- 删除或修改操作会写入审计日志。

管理员只能编辑和删除已授权 VLAN 的记录；普通用户没有写入权限。

### 3. 查询与排序

IP 台账支持：

- 按 IP、设备名称、使用人、MAC 地址搜索；
- 按 VLAN、部门、状态、设备类型筛选；
- 勾选“只看重复 IP”；
- 点击表头按字段升序或降序排序；
- IP 地址按数值排序，而不是按字符串排序；
- 按当前筛选条件导出全部结果。

## 五、网段明细

1. 进入“网段明细”。
2. 选择要查看的网段前缀。
3. 查看该网段的已登记记录、已使用数量、可用数量和未登记主机号。
4. 可按主机号、IP、设备名称、部门、使用人和状态排序。
5. 点击查看图标查看单条记录详情。
6. 点击导出可导出当前网段记录。

剩余可用数依据 VLAN 规划中的可用地址范围计算，并扣除网关地址。若规划发生变化，统计结果也会随之变化。

## 六、数据导入

### 1. 准备文件

支持 `.xlsx` 和 `.csv` 文件。第一行应为列名，建议使用以下字段：

- IP 地址
- MAC 地址
- 设备名称
- 部门
- 使用人
- 设备类型
- 使用状态
- VLAN
- 网关
- 向日葵 ID
- 备注

向日葵 ID 等字段会按文本处理，以避免前导零丢失。

### 2. 执行导入

1. 进入“数据导入”。
2. 选择 Excel 或 CSV 文件。
3. 检查系统显示的列名映射。
4. 选择重复 IP 处理策略：
   - 跳过：保留原记录；
   - 更新：覆盖匹配的原记录；
   - 报错：重复行不导入并显示原因。
5. 点击导入并等待结果。
6. 查看新增、更新、跳过和错误数量；必要时根据行号和原因修正原文件后重新导入。

当 VLAN 为空或填写“自动获取”时，系统会根据 IP 前缀识别 VLAN。导入仍会执行 IP 格式、VLAN 一致性、地址池和权限校验。管理员只能导入授权 VLAN 内的数据。

导入前应备份数据库，尤其是在选择“更新”策略时。

## 七、数据导出

1. 在 IP 台账中设置搜索和筛选条件。
2. 点击“导出”，系统会导出当前筛选条件下的全部记录，而不是仅当前页。
3. 在网段明细中导出时，系统只导出当前网段。
4. 使用 Excel 打开 CSV 时应确认编码正确。

导出文件可能包含设备、使用人和网络信息，请按照组织的数据安全规定保存和传输。

## 八、修改密码

1. 点击顶部栏的修改密码入口。
2. 输入当前密码。
3. 输入两次新密码。
4. 新密码至少 6 位，确认无误后保存。
5. 修改成功后使用新密码重新登录。

不要使用容易猜测的密码，也不要在多个系统复用相同密码。

## 九、常见问题

### 看不到某个 VLAN 的记录

管理员可能尚未获得该 VLAN 的授权。联系超级管理员检查账号的 VLAN 权限和记录所属 VLAN。

### 新增或导入时提示 IP 不在地址池

检查 VLAN 规划中的可用地址范围、IP 主机号和网关配置。确认 IP 确实属于该 VLAN 的子网。

### 导入出现大量错误

先查看错误行号和具体原因，检查列名、IP 格式、字典值、VLAN、地址池和重复策略。建议拆分文件后分批导入。

### 忘记密码

联系超级管理员使用用户管理中的“重置密码”功能。超级管理员忘记密码时，应由具备服务器访问权限的维护人员按组织流程处理，不要直接修改生产数据库而不留记录。

### 页面无法访问

确认服务已启动、端口未被占用、数据库已初始化，并检查服务端日志。生产环境还需确认反向代理和 HTTPS 配置正常。

## 十、安全与运维要求

- 首次部署后立即修改默认超级管理员密码。
- 使用以下命令生成 Session 密钥，并写入项目根目录 `.env`：

  ```bash
  echo "SESSION_SECRET=$(openssl rand -hex 32)" >> .env
  ```

- 不要把 `.env`、数据库文件、导出文件或备份文件提交到 Git 或公开目录。
- 生产环境使用 HTTPS，并限制管理端口和数据库文件权限。
- 定期备份 `db/ipam.db`，备份文件应加密并测试恢复流程。
- 导入外部文件前确认来源，避免将不可信文件直接用于生产导入。
- 定期审查用户账号、管理员 VLAN 权限和审计日志。
- 变更 VLAN 规划、网关映射前先确认对现有 IP 记录和导入流程的影响。

## 十一、联系与升级

系统升级前应：

1. 导出重要台账数据；
2. 备份 `db/ipam.db` 和 `.env`；
3. 记录当前 Node.js 和依赖版本；
4. 在测试环境验证数据库初始化、登录、导入和导出；
5. 确认升级后审计日志和权限策略仍符合组织要求。
